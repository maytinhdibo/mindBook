<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/detail_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    style="@style/container"
    android:paddingBottom="@dimen/bottom_nav"
    >

    <LinearLayout
        android:visibility="gone"
        android:id="@+id/location_req"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        style="@style/container"
        android:gravity="center_horizontal"
        android:layout_marginTop="8pt"
        android:paddingTop="8pt"
        android:paddingBottom="8pt"
        android:background="@drawable/review_item"
        >
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="18sp"
            android:textAlignment="center"
            android:text="@string/req_location_des"
            />
        <Button
            android:id="@+id/open_location_cog"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/detail_button"
            android:layout_marginTop="8pt"
            android:paddingRight="8pt"
            android:paddingLeft="8pt"
            android:text="@string/req_location"/>
    </LinearLayout>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/near_list"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            />

</LinearLayout>